# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

# Comment the next line if you don't want to use dynamic frameworks
use_frameworks!
def pods_list
    pod 'SwiftLint'
    pod 'Firebase/Core'
    pod 'Firebase/DynamicLinks'
end

target 'DEM app' do
  # Pods for DEM app
  pods_list

  target 'DEM appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DEM appUITests' do
    # Pods for testing
  end

end
